// Import express framework for Node.js
const express = require('express')

// Creates an instance of an Express router
const router = express.Router()

// Exports the router instance, making it available for use in other parts of your application
module.exports = router